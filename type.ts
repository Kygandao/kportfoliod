import { IconType } from "react-icons";

export interface IOffer{
    title: string,
    about: string,
    Icon: IconType
}

export interface ISkills{
    name: string,
    level: string,
    Icon: IconType
}